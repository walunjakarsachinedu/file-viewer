name: pdf
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  receive_intent: ^0.2.3
  pdf_viewer_plugin: ^2.0.1
  uri_to_file_path:
    git:
      url: https://github.com/walunjakarsachinedu/uri-to-file-path.git
      ref: main # branch name
  receive_sharing_intent: ^1.4.5
  permission_handler: ^10.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # assets:
  #   - asset/sample.pdf
  #   - images/a_dot_ham.jpeg
